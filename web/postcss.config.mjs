/** @type {import('postcss-load-config').Config} */
// NOTE: Tailwind CSS v4 is handled via the @tailwindcss/vite Vite plugin (astro.config.ts).
// This file is intentionally empty — no PostCSS plugin needed for tailwindcss v4 with Vite.
const config = {
  plugins: {},
}

export default config
