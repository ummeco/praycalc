# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
dist/
!smart/vendor/pray-calc/dist/
build/
.vercel/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Claude AI working memory
.claude/

# Flutter
**/flutter/.dart_tool/
flutter/**/.dart_tool/
**/flutter/.flutter-plugins
**/flutter/.flutter-plugins-dependencies
**/flutter/build/
flutter/.packages
flutter/.metadata
flutter/android/.gradle/

# Firebase / GCP config (contains API keys)
**/GoogleService-Info.plist
**/google-services.json

# Android signing
**/key.properties
**/upload-keystore.jks
**/*.keystore

# Swift/Xcode build artifacts
**/.build/
DerivedData/
*.pcm
*.o
*.d

# Wear OS / Android build
wearos/.gradle/
wearos/app/build/
wearos/app/.cxx/
wearos/build/
wearos/local.properties

# Expo managed-workflow generated native projects (mobile/ — prebuild output, never committed)
mobile/android/
mobile/ios/
.clangd
**/.clangd

# Other AI editors
.codex/
.cursor/
.aider/
.aider.chat.history.md
.continue/
.windsurf/
.gemini/
.codeium/
.vscode/*
.vercel
.env*.local

# Secrets — defense-in-depth (P8 HYGIENE-gitignore T02)
*.pem
*.key
id_rsa*
id_ed25519*
credentials.json
service-account*.json
google-service-account.json
vault.env
*.p12
*.pfx
*.p8
asc-api-key*

# AI tools (extended set) — P8 HYGIENE-gitignore T03
.copilot/
.claire/
.zed/
.ai/
.gpt/
.anthropic/
.openai/
.github/copilot/
AGENTS.md

# OS junk (extended) — P8 HYGIENE-gitignore T04
desktop.ini
*~
ehthumbs.db

# Flutter macOS Pods (generated, not committed)
flutter/macos/Pods/
mobile/.expo/

# Lighthouse CI report output (quality-gates.yml lighthouse/lighthouse-org jobs)
.lighthouseci/

# web/vendor/*/dist are required at build time via file: deps (no build step) —
# exempt them from the generic dist/ ignore so a fresh checkout can build.
!web/vendor/*/dist
