org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Local dev only: if you get "Unsupported class file major version" errors (Java 25 via VS Code),
# add org.gradle.java.home to your USER-LEVEL ~/.gradle/gradle.properties — never commit it here.
# CI sets JAVA_HOME via actions/setup-java and does not need this override.
