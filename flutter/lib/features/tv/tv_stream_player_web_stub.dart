import 'package:flutter/material.dart';

/// Non-web stub — iframe embedding is not supported on this platform.
Widget buildYouTubeIframe(String embedUrl, String viewId) =>
    const SizedBox.shrink();

/// Non-web stub — HLS video player is not available on this platform.
Widget buildHlsVideoPlayer(String hlsUrl, String viewId, {bool muted = true}) =>
    const SizedBox.shrink();
