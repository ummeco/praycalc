# praycalc/smarthome/alexa/lambda — Alexa skill fulfillment Lambda env vars
# Copy to .env.local for local testing with ask-sdk

# PrayCalc backend
UMMAT_BACKEND_URL=https://api.praycalc.com
HASURA_ADMIN_SECRET=  # from vault: HASURA_GRAPHQL_ADMIN_SECRET (server-side only)

# Alexa skill registration (ummeco Amazon developer account per D-S19-04)
ALEXA_SKILL_ID=  # from vault: PRAYCALC_ALEXA_SKILL_ID
